import React from "react";
// import { Fragment } from "react";
// import { Navigate } from "react-router-dom";
// import userExist from "../hooks/useLocalStorage";

// //一个简单的鉴权操作
// export const AuthWrapComponent = ({
//   children: any,
// }) => {
//   const [token, _] = useLocalStorage("token");
//   return (
//     <Fragment>
//       {/* 注意一定要replace ,导航到404空白页 或者 401无权*/}
//       {token ? (
//         children
//       ) : (
//         <Navigate to="/a/about" replace />
//       )}
//     </Fragment>
//   );
// };
